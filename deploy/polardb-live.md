# PolarDB PostgreSQL 版 —— 真连实测记录

这份文档只回答一个问题：**MAOS 的 PG 通道要用到的能力，在一台真实的
PolarDB PostgreSQL 版实例上，到底哪些是跑过的、哪些没跑过。**

它存在的理由是：材料里关于 PolarDB 的说法此前只有一句**推理** ——
「PolarDB 兼容 PG 协议 + pgvector 扩展可用，所以能跑」。推理不是实测。
本文件负责把那句话换成一条一条可复核的记录，**包括没跑通的那些**。

## 与 `deploy/polardb.md` 的分工（别把两份文档混起来）

| 文件 | 回答什么 | 天花板 |
| :-- | :-- | :-- |
| `deploy/polardb.md`（T10 轨） | MAOS 的 PG 后端怎么接、怎么配、怎么降级 | 本机 Docker `pgvector/pgvector:pg16` |
| **本文件**（T14 轨） | **真 PolarDB 实例上，地基能力实际跑通了哪几条** | 不碰 MAOS 源码，只验数据库这一侧 |

> 撰写时 `deploy/polardb.md` 尚未落到主干（T10 轨交付中）。凡涉及
> **中文分词口径**、**降级口径**的结论，一律以该文件为准，本文件只出实测数据。

🔴 **一条硬纪律：本文件里「本机 pgvector 跑通」和「PolarDB 跑通」是两件事，
分栏写、不混说。** 前者只能证明脚本本身没问题，证明不了云上那台机器行不行。

---

## 一、已实测

### 1.1 对照组：本机 Docker pgvector —— 五步全绿 ✅

**这一栏不是 PolarDB 的结论**，它的作用是排除「脚本本身有问题」这个可能：
先在一台已知可用的库上跑绿，之后连 PolarDB 再失败，才能归因到 PolarDB。

- 环境：`pgvector/pgvector:pg16` 容器（`deploy/docker-compose.yml` 的 `pg` profile）
- 命令：`python3 scripts/polardb_smoke.py --local`
- 结果：**5/5 步通过，exit=0**

```
====================================================================
PolarDB / pgvector 地基冒烟 —— 五步
====================================================================
驱动：psycopg 3.3.4
目标：本机 pgvector（--local）
--------------------------------------------------------------------
  [ OK ] 1. 连接 + SELECT version() -> PostgreSQL 16.15 (Debian 16.15-1.pgdg12+2) on aarch64-unknown-linux-gnu, compiled by gcc (Debian 12.2.0-14+deb12u1) 12.2.0, 64-bit
  [ OK ] 2. CREATE EXTENSION vector -> pgvector 0.8.6
  [ OK ] 3. 全文 to_tsvector + ts_rank -> 命中 ['d1']，ts_rank=0.099103（越大越相关）
  [note]    （观察）中文 to_tsvector('simple') -> '退款订单已经超时未处理':1
  [ OK ] 4. 向量 vector 列 + <=> 排序 -> top-1=a，余弦距离 a=0.000000, c=0.006116, b=1.000000（越小越相关）
  [ OK ] 5. 清理临时表 -> maos_smoke_fts / maos_smoke_vec 已删
--------------------------------------------------------------------
结论：5/5 步通过
exit=0
```

五步各自验的是什么，以及断言强度：

| # | 验什么 | 断言（不是「没报错就算过」） |
| :-- | :-- | :-- |
| 1 | 连接 + `SELECT version()` | 拿到版本串 |
| 2 | `CREATE EXTENSION IF NOT EXISTS vector` | 且能从 `pg_extension` 读出 `extversion` |
| 3 | `to_tsvector` + `ts_rank` | 命中集合**必须恰好等于** `['d1']`（`fox & brown` 只有 d1 同时含两词），且 `ts_rank > 0` |
| 4 | `vector` 列 + `<=>` + 排序 | 构造已知相似度，**全序必须恰好等于** `['a','c','b']` |
| 5 | 清理 | 两张自建表删净，不留垃圾 |

### 1.2 PolarDB 实例上的五步 —— 五步全绿 ✅

**补跑日期：2026-08-30。** 阿里云 PolarDB PostgreSQL 版，按量付费，走公网地址 +
白名单放行本机出口 IP。连接串只经环境变量传入，本文件不含任何真实值。

```
====================================================================
PolarDB / pgvector 地基冒烟 —— 五步
====================================================================
驱动：psycopg 3.3.4
目标：环境变量 MAOS_PG_DSN
--------------------------------------------------------------------
  [ OK ] 1. 连接 + SELECT version() -> PostgreSQL 16.14 (PolarDB 16.14.20.0 build 1f03f15d) on x86_64-linux-gnu
  [ OK ] 2. CREATE EXTENSION vector -> pgvector 0.8.3.1
  [ OK ] 3. 全文 to_tsvector + ts_rank -> 命中 ['d1']，ts_rank=0.099103（越大越相关）
  [note]    （观察）中文 to_tsvector('simple') -> '退款订单已经超时未处理':1
  [ OK ] 4. 向量 vector 列 + <=> 排序 -> top-1=a，余弦距离 a=0.000000, c=0.006116, b=1.000000（越小越相关）
  [ OK ] 5. 清理临时表 -> maos_smoke_fts / maos_smoke_vec 已删
--------------------------------------------------------------------
结论：5/5 步通过
exit=0
```

**2026-08-31 开启 SSL 后原样重跑，输出逐字一致**：同一版本串
`PolarDB 16.14.20.0 build 1f03f15d`、同一 `pgvector 0.8.3.1`、`ts_rank=0.099103`、
余弦距离三个值全同，5/5、`exit=0`。即**加密切换没有改变任何一步的结果** ——
这正是重跑要证明的事：SSL 是链路层的事，不该动到任何查询语义；
真动了才说明有别的东西被一起改了。

🔴 **但第一次跑出来是 2/5，不是 5/5。** 这条必须记 —— 照着做的人会撞同一堵墙，见 §1.3。

### 1.3 前置条件：普通账号跑不完这五步（第一次 2/5 的实录）

控制台建的**普通账号**连上去，第 2、3 步当场挂掉：

```
  [ OK ] 1. 连接 + SELECT version() -> PostgreSQL 16.14 (PolarDB 16.14.20.0 build 1f03f15d) on x86_64-linux-gnu
  [FAIL] 2. CREATE EXTENSION vector -> InsufficientPrivilege: permission denied to create extension "vector"
            HINT:  Must be superuser or user with all of polar_superuser to create this extension.
  [FAIL] 3. 全文 to_tsvector + ts_rank -> InsufficientPrivilege: permission denied for schema public
  [FAIL] 4. 向量 vector 列 + <=> 排序 -> 跳过：第 2 步没拿到 vector 扩展
  [ OK ] 5. 清理临时表 -> maos_smoke_fts / maos_smoke_vec 已删
结论：2/5 步通过   exit=1
```

**这是两个互相独立的坑，别当成一件事**（只修其中一个，另一个照样挡住）：

| 症状 | 根因（实测） | 修法 |
| :-- | :-- | :-- |
| 装不了 `vector` | 普通账号 `rolsuper=f` 且不属于任何角色；PolarDB 要求 `polar_superuser` | 控制台「账号管理 → 创建账号 → **高权限账号**」，用它执行一次 `CREATE EXTENSION` |
| `public` 里建不了表 | PG15 起 `public` 的 ACL 是 `{pg_database_owner=UC/pg_database_owner,=U/pg_database_owner}`，普通用户只有 `USAGE` 没有 `CREATE`；且本库 owner 是 PolarDB 内建的 `aurora`，普通账号对库也没有 `CREATE`，**连自建一个模式绕开都不行** | 高权限账号执行 `GRANT CREATE ON SCHEMA public TO <普通账号>` |

高权限账号跑完这两条 DDL 之后，**后续全程用普通账号**即可 —— 灌 schema、跑测试都不再需要高权限。

账号盘点（实测 `pg_roles`）：`aurora` / `polardb_admin` / `replicator` 三个是 PolarDB
**内建**超级用户，口令不归使用者掌握，**别指望用它们**；控制台新建的高权限账号属于
`pg_polar_superuser` 角色，这才是能用的那个。

---

### 1.4 中文分词：zhparser 在 PolarDB 上装成了，中文真的切开了

`deploy/polardb.md` 原先推断「托管实例大概率装不了中文分词扩展」。上一轮只验到
「在可用扩展列表里」，**这一轮把它装了**：

```
CREATE EXTENSION zhparser                                      -> zhparser 2.2   (0.03s)
CREATE TEXT SEARCH CONFIGURATION zhcfg (PARSER = zhparser)     -> OK             (0.01s)
ALTER TEXT SEARCH CONFIGURATION zhcfg ADD MAPPING FOR n,v,a,i,e,l WITH simple    (0.01s)
```

同一句话，两个配置的分词结果：

```
simple  ->  '退款政策超时未到账':1                      ← 整句一个 token
zhcfg   ->  '退款':1 '政策':2 '超时':3 '到':4 '账':5     ← 真切开了
```

装扩展要高权限账号（与 `vector` 同理，见 §1.3）；**建配置之后普通账号不用任何授权
就能用 `zhcfg`**，配套的 GIN 索引也由普通账号建（表是它建的，它是 owner）。

#### 召回实测：24 条真语料，三条通道对比

语料是 `scenarios/refund/history/`（24 条历史案例，真中文），
灌进 `kb_doc_pg` 并用 `maos/kb/retriever.py` 的 `embed()` 现算 64 维向量。
「应命中」由标题人工核定，不是事后凑的。

| 查询 | 应命中 | `simple` 全文 | `zhcfg` 全文 | 向量通道 top-3 |
| :-- | :-- | :-- | :-- | :-- |
| 无理由退货 | 0001, 0009 | 抛 `LookupError` | **0009, 0001** | 0001, 0009, 0012 |
| 余额不足 | 0019 | 抛 `LookupError` | **0019** | 0019, 0002, 0010 |
| 人为损坏 举证 | 0008, 0016 | 抛 `LookupError` | **0016, 0008** | 0016, 0008, 0009 |
| 调用频次超限 | 0018 | 抛 `LookupError` | **0018** | 0018, 0020, 0017 |
| 折旧 | 0014 | 抛 `LookupError` | **0014** | 0003, 0011, 0012 |
| 渠道商核销 | 0003, 0011, 0015 | 抛 `LookupError` | 0003 | 0003, 0011, 0012 |

**合计：`zhcfg` 全文 8/10，向量 top-5 10/10，`simple` 一条都查不了（全部抛错）。**

三条结论，各自的依据都在上表里：

1. **装 zhparser 把中文全文通道从「完全不可用」变成「可用」。** `simple` 那一列六条
   全是抛错 —— 不是召回差，是这条通道压根走不通（`_port_search` 一次 CJK 查询就把
   它永久标记不可用，见 `polardb.md`）。
2. **`zhcfg` 漏的那两条是 `plainto_tsquery` 的 AND 语义，不是分词的锅。**
   「渠道商核销」被切成 `渠道商 & 核销`，而 0011／0015 的原文是「经销渠道…核销」，
   没有「渠道商」这个词，AND 下必然不命中。全文通道对同义词无能，这是它的本性。
3. **两条通道互补，不是替代关系。** 向量 top-5 全中（同义词靠语义捞回来），但排序噪声大
   ——「折旧」这条的前 3 名全错，正确答案在第 4-5 名；而 `zhcfg` 把它排在第 1。

### 1.5 HNSW 向量索引：规模性能与召回

数据是**合成**的（真语料的 210 条句子随机重组），向量走的是真实的 `embed()`，
所以向量分布是这套系统的真实分布，不是随机噪声。🔴 **这一栏的数字来自合成数据，
不要当成真业务负载的结论** —— 真语料只有 24 条，撑不起规模测试。

时间一律取 `EXPLAIN (ANALYZE)` 的服务端 `Execution Time`，**不含公网往返**
（家宽到北京 RTT 几十毫秒，会把 0.5ms 的差别整个淹掉）。查询 30 条，与库内文档同分布。

| 规模 | 通道 | 计划 | p50 | p95 | 召回@10 | 相对顺序扫描 |
| :-- | :-- | :-- | --: | --: | --: | --: |
| **5 万行** | 顺序扫描（精确解） | Seq Scan | 16.1 ms | 17.6 ms | — | 1.0x |
| | HNSW `ef_search=10` | Index Scan | 0.29 ms | 0.39 ms | 89.7% | 56x |
| | HNSW `ef_search=40`（缺省） | Index Scan | 0.58 ms | 0.82 ms | **99.3%** | 28x |
| | HNSW `ef_search=100` | Index Scan | 1.06 ms | 1.43 ms | 99.7% | 15x |
| | HNSW `ef_search=400` | Index Scan | 2.80 ms | 4.16 ms | 99.7% | 6x |
| **20 万行** | 顺序扫描（精确解） | Seq Scan | 72.5 ms | 79.8 ms | — | 1.0x |
| | HNSW `ef_search=10` | Index Scan | 0.33 ms | 0.53 ms | 85.0% | 220x |
| | HNSW `ef_search=40`（缺省） | Index Scan | 0.62 ms | 0.86 ms | **99.3%** | 117x |
| | HNSW `ef_search=100` | Index Scan | 1.19 ms | 1.70 ms | 99.7% | 61x |
| | HNSW `ef_search=400` | Index Scan | 3.87 ms | 4.62 ms | 99.7% | 19x |

构建与体积（`m=16, ef_construction=64`，pgvector 0.8.3.1 的缺省值，未调参）：

| 规模 | 建索引耗时 | 索引大小 | 堆表 | COPY 入库（含公网传输） |
| :-- | --: | --: | --: | --: |
| 5 万行 | 8.7 s | 27 MB | — | 23.6 s（2122 行/秒） |
| 20 万行 | **77.9 s** | 109 MB | 123 MB | 198.1 s（757 行/秒） |

四条结论：

1. **HNSW 的查询延迟几乎不随规模增长**：数据涨 4 倍（5 万 → 20 万），`ef_search=40`
   的 p50 从 0.58ms 只到 0.62ms；而顺序扫描从 16.1ms 涨到 72.5ms（线性）。
   所以**加速比随规模放大**：28x → 117x。这是建这条索引的全部理由。
2. **建索引耗时是超线性的**：数据 4 倍，耗时 9 倍（8.7s → 77.9s）。10 倍数据时别按
   线性估工期。
3. **索引不小**：109 MB 索引 vs 123 MB 堆表，接近 1:1。64 维尚且如此，换更高维的
   嵌入模型要重新算存储账。
4. 🔴 **`ef_search=10` 不要用**：召回 85%–90%，而且**不稳定** —— 同一份数据、同一组
   查询，换一次索引构建就能从 100% 掉到 89.7%（本轮实测到过这个跨度，见 §3.6）。
   `ef_search=40`（pgvector 缺省）在两个规模上都稳定在 99.3%，延迟只有 0.6ms，
   **没有理由往下调**。

> ✅ **代码侧已于 2026-08-31（T34 轨）闭环**：`PgStorePort.connect()` 现在显式
> `SET hnsw.ef_search`（缺省 40，`MAOS_PG_HNSW_EF_SEARCH` 可覆盖），不再吃服务端缺省。
> 上表那些数字因此从「这台实例恰好的表现」变成了「代码钉住的行为」——
> 换实例、换 pgvector 版本、或有人在实例参数里改了这个值，召回都不会再静默漂移。
> 实测见 §3.7。

## 二、未实测

### 2.1 ~~PolarDB 实例上的全部五步 —— 未实测~~ → 已于 2026-08-30 补跑，见 §1.2 ✅

原因存档（这条坑留着，因为它在补跑当天又出现了一次）：首轮之所以没跑成，是因为
交给会话的 `MAOS_PG_DSN` 是派单 §1 里的占位符模板原文
`postgresql://<user>:<pass>@<host>:<port>/<db>`，五个尖括号一字未改。
它暴露的方式很绕：

1. 开场自检第 5 条只验「环境变量非空」，占位符模板照样报「**已配置**」；
2. 于是第一次连接失败表现为一个指向不明的 `OperationalError` ——
   看起来像白名单没放行，实际上根本没有 host 可连；
3. 分层诊断（DNS → TCP → PG 鉴权）时才在解析端口那一步撞出
   `ValueError: Port could not be cast to integer value as '<port>'`。

**已把等价检测内建进 `scripts/polardb_smoke.py`**：DSN 含尖括号占位符时当场报
「没替换成真实连接串」并 `exit=2`，不再伪装成连接失败。派单自检那一行的
同类修法已记进 `docs/BACKLOG.md ## task-T14`。

**这条检测在补跑当天真拦下了一次**：高权限账号那份 DSN 只换了口令、用户名那格还留着
`<ADMIN_USER>`，检测当场点名「还有占位符没换」，没有再伪装成一次鉴权失败。

### 2.2 原先挂着的四个问号 —— 已全部答掉

| 原问题 | 实测答案（2026-08-30） |
| :-- | :-- |
| `CREATE EXTENSION vector` 能不能装成 | ✅ **能**，但必须用高权限账号，见 §1.3 |
| PG 大版本与 pgvector 版本各是多少 | `PostgreSQL 16.14 (PolarDB 16.14.20.0 build 1f03f15d)` on x86_64-linux-gnu；**pgvector 0.8.3.1** |
| `<=>` 行为是否与本机 pgvector 一致 | ✅ **逐位一致**，见 §3.2 |
| 是否要先在控制台「插件管理」里启用 | ❌ **不需要**。`vector` 本来就在 `pg_available_extensions` 里（该实例共 **189** 个可用扩展），瓶颈只有账号权限这一条 |

> 因此复赛材料里关于 PolarDB 的表述**可以写「已在 PolarDB PostgreSQL 版实例上实测」**，
> 并附 §1.2 的输出。仍然**不能写**的是性能、并发、大数据量、主备切换相关的任何结论 —— 见 §3.4。

### 2.3 复跑方式（一条命令）

脚本设计成**不 import maos**，任何人拿一条 DSN 就能复跑，不需要装 MAOS 本体：

```bash
python3 -m pip install 'psycopg[binary]'        # 刻意不进 pyproject 的 dependencies
export MAOS_PG_DSN='postgresql://真实连接串'    # 只走环境变量，不落任何文件
python3 scripts/polardb_smoke.py
```

§1.2 那段输出就是这么来的。脚本输出**自带脱敏**（host／口令／用户名／库名在打印前
已被抹成 `<redacted>`），所以它的输出可以直接进文档和材料，不必人工再过一遍。

🔴 换一台新实例复跑时，**先照 §1.3 把高权限账号那两条 DDL 做掉**，否则拿到的是 2/5。

---

## 三、已知差异 / 局限

### 3.1 中文分词：PG 自带分词器不切中文（本机实测现象）

第 3 步的观察项实测到：

```
to_tsvector('simple', '退款订单已经超时未处理')  ->  '退款订单已经超时未处理':1
```

**整句被当成了一个 token**（`:1` 是它的位置），没有任何切分。
这不是配置错误，是 PG 默认分词器不认中文 —— 换 `'english'` 配置结果同理。

其后果是：**中文语料上的全文检索基本只能命中「整句完全一致」的查询**，
`ts_rank` 的排序在中文上不具备可用的区分度。

同一条在 **PolarDB 上复测，现象完全相同**：

```
to_tsvector('simple', '退款政策超时未到账')  ->  '退款政策超时未到账':1
```

> 🔴 **口径以 T10 的 `deploy/polardb.md` 为准，本文件只出现象、不下结论。**
> 常见的三条出路（装 `zhparser`／`pg_jieba` 扩展、应用层预分词后写入、
> 全文通道只用于英文与 ID 类字段而中文走向量通道）各自的取舍属于 T10 的面。

**但这条局限在 PolarDB 上是可解的** —— `deploy/polardb.md` 当时推断「托管实例大概率
装不了中文分词扩展」，实测推翻了：可用扩展列表里四个都在，且 `zhparser` **已经装上并
实测通过**（§1.4）。

| 扩展 | 可用版本 | 本轮是否安装 |
| :-- | :-- | :-- |
| `zhparser` | 2.2 | ✅ **已装并实测**，见 §1.4 |
| `pg_jieba` | 1.1.2 | 否（在可用列表里） |
| `pg_bigm` | 1.2 | 否（在可用列表里） |
| `pgroonga` | 4.0.5 | 否（在可用列表里） |

所以 §3.1 这条局限的准确表述是：**PG 内置配置不切中文，这一点在 PolarDB 上同样成立；
但 PolarDB 允许装 `zhparser`，装完这条局限就解除了**。未装的那三个仍然只验到
「在可用列表里」，没装过。

### 3.2 PolarDB 与本机 pgvector 的行为差异 —— 检索行为零差异，差异在别处

**五步里的每一个数值逐字节相同**（左：本机 Docker `pgvector/pgvector:pg16`；
右：PolarDB 16.14）：

| 项 | 本机 pgvector | PolarDB | 差异 |
| :-- | :-- | :-- | :-- |
| 全文命中集 | `['d1']` | `['d1']` | 无 |
| `ts_rank` | `0.099103` | `0.099103` | 无 |
| `<=>` 全序 | `a, c, b` | `a, c, b` | 无 |
| 余弦距离 | `0.000000 / 0.006116 / 1.000000` | `0.000000 / 0.006116 / 1.000000` | 无 |
| 中文 `to_tsvector('simple')` | 整句一个 token | 整句一个 token | 无（见 §3.1） |

差异全在检索行为之外的这三条：

| 项 | 本机 pgvector | PolarDB | 影响 |
| :-- | :-- | :-- | :-- |
| pgvector 版本 | 0.8.6 | **0.8.3.1**（旧两个小版本） | 本层只用 `vector` 类型与 `<=>`，两版实测无差 |
| PG 版本串 | `16.15 (Debian ...)` 社区版 | `16.14 (PolarDB 16.14.20.0 build 1f03f15d)` | 版本串本身就是「这是不是真 PolarDB」的判据 |
| 权限模型 | 容器里的账号是 superuser，五步随便跑 | 普通账号跑不完，需高权限账号先做两条 DDL | 见 §1.3 —— **这正是本机对照组测不出来的那一类问题** |

### 3.3 分数方向：两个通道相反，接 StorePort 时必须转换

本机实测到的方向（PolarDB 上预计相同，但**未实测**）：

| 通道 | 算子 | 原始值方向 | 实测样例 |
| :-- | :-- | :-- | :-- |
| 全文 | `ts_rank(...)` | **越大越相关** | 命中 d1 → `0.099103` |
| 向量 | `<=>`（余弦距离） | **越小越相关** | `a=0.000000, c=0.006116, b=1.000000` |

契约 F-2 附则要求两个后端的 `fts_search` / `vector_search` 都返回
**「越大越相关」** 的分数。所以向量通道在实现侧必须做一次方向翻转
（例如 `1 - (embedding <=> query)`），**否则排序会整个倒过来，而两边的测试
各自都可能是绿的** —— 这属于 T10 实现面的事，此处只记录方向差异这个事实。

### 3.6 HNSW 低 `ef_search` 的召回随索引构建波动 —— 一条测量过程本身的教训

本轮 §1.5 那张表里 `ef_search=10` 的召回是 89.7%（5 万行）／85.0%（20 万行）。
但中途有三次独立测量在**同一份数据、同一组查询**上拿到 100%。差别只有一个：
那三次用的是**上一次构建出来的索引**，而最终表里的数字来自 `DROP INDEX` 后
**重新构建**的索引。

结论：**低 `ef_search` 下，召回率是「这一次索引怎么建出来的」的函数，不是数据的函数。**
HNSW 的构建有随机性，图的连通性每次不同，低 `ef` 的贪心搜索对此敏感；
`ef_search ≥ 40` 时两个规模、多次构建都稳定在 99.3%–99.7%。

这条之所以写进文档，是因为它差点变成一个错误结论：中间那三次 100% 如果先出现、
且没有重建索引再验一遍，就会得到「HNSW 在这个规模上无损」这个**过于漂亮而错误**的结论。
凡是「换一次构建就变」的数字，单次测量都不算数。

### 3.7 `hnsw.ef_search` 已从「环境的缺省」变成「代码的选择」（2026-08-31 实测）

§3.6 讲的是这个值**不能乱调**，本节讲的是它**此前根本没被设过**。

改动前 `maos/store/pg_store.py` 完全依赖服务端缺省值。危害不在于缺省值不好
（40 恰好是对的），而在于**它是环境的属性而不是代码的属性**：换一台实例、换一个
pgvector 版本、或有人在实例参数里改了它，向量召回会**静默变化 —— 不报错、不变慢、
不留痕迹，只是结果悄悄变差**。这是最难被发现的一类退化，也正是铁律 8 要防的那类假象。

改动：`PgStorePort.connect()` 拿到连接后显式 `SET hnsw.ef_search`，
缺省 `40`，`MAOS_PG_HNSW_EF_SEARCH` 可覆盖。实测（本机 pgvector 容器，
`pgvector/pgvector:pg16` + vector 0.8.6）：

```
代码里的缺省值        : 40
会话里 SHOW 出来的值  : {'hnsw.ef_search': '40'}     ← SET 真的到了会话上
覆盖成 77 后 SHOW     : {'hnsw.ef_search': '77'}     ← 环境变量确实生效
非法值 ''    -> 回落到 40
非法值 'abc' -> 回落到 40
非法值 '0'   -> 回落到 40
非法值 '-5'  -> 回落到 40
```

**`SET` 失败时只记 warning、不抛**，这是刻意的，且不违反 `connect()` 那句
「绝不回落 sqlite」：那条契约管的是**后端身份**（选了 postgres 就必须是 postgres），
而 `SET` 失败时后端身份没有任何变化 —— 仍是真 postgres，全文／KV／向量通道全都照常，
向量召回只是回到服务端缺省，也就是这次改动之前的现状。为一个调优参数抛错会把整个
store 打死、连不碰向量的调用方一起打死，「装了 PG 但没装 pgvector」的部署会因此完全
不能用 —— 那是比问题本身大得多的破坏。静默吞掉则回到了正要治的病。warning 这个中间档
正好卡在两者之间，与 `verify.py` 对沙箱降级的处置是同一个取舍。

> 顺带一个 PG 机制上的便宜：`SET hnsw.ef_search` 在 pgvector **尚未加载**的会话里
> 也不会报错 —— 带点号的自定义 GUC 会被存成 placeholder，等扩展加载时再套用。
> 所以这条 `SET` 不需要「先确认扩展装了」这个前置。

### 3.8 白名单不放行 = TCP 静默超时，不是拒绝（冒烟脚本现在会自己点名）

**症状与「网络不通 / 实例没起来 / host 写错了」完全一样**：`dig` 能解析出公网 IP，
`connect()` 挂满超时然后抛一个指向不明的异常。而冒烟脚本第 1 步刻意只报驱动异常
**类名**、不报 message（防 host 泄漏），所以从脚本输出上**更看不出**是白名单。
这一条历史上吃掉过整轮排障往返。

2026-08-31（T34 轨）给第 1 步加了失败后的分层诊断，**当轮就现场命中**：

```
  [FAIL] 1. 连接 + SELECT version() -> 连不上：ConnectionTimeout
  [note] 诊断 DNS -> 解析成功，1 个 A 记录
  [note] 诊断 TCP -> 静默超时（8s）
  [note] 诊断结论 -> DNS 通 + TCP 静默超时 = **大概率是白名单没放行本机出口 IP**。
                     本机出口 IP: <本机公网 IP> —— 去控制台把它加进白名单再复跑。
```

判据是三选一，不是只会喊白名单：

| 现象 | 结论 |
| :-- | :-- |
| DNS 解析失败 | host 写错了 |
| DNS 通 + TCP **静默超时** | **大概率白名单没放行**，附本机出口 IP |
| DNS 通 + TCP **被拒绝** | 端口没在听（实例停了 / 端口写错），**不是**白名单 |
| DNS 通 + TCP 握手成功 | 网络没问题，问题在 PG 鉴权层（账号 / 库名 / SSL 策略） |

🔴 **触发这条的常见原因不是「忘了配白名单」，而是出口 IP 变了** —— 白名单按出口 IP
放行，家用宽带 / 移动网络的出口 IP 会变。**上一轮能连上不代表这一轮能连上**，
换网络、重拨、隔一天回来都可能要重加一次。

诊断全程不打印 host，也不打印解析到的 IP（那是目标 host 的地址，打出来等于泄漏）——
只报「解析到几个 A 记录」。出口 IP 是本机的、不泄漏目标，所以可以打，而它恰好正是
人去控制台要填的那个值。这几条同样走 `rep.note()`，不改断言、不改退出码。

### 3.5 SSL：从「不支持」到已开启（含开启前的明文期实录）

🔴 **口令未轮换 —— 明文期的暴露没有被撤销**，见本节末尾「未闭环」。

#### 开启前（2026-08-30 实测）

实测（客户端侧 libpq 的判定 + 服务端参数，两边互相印证）：

```
sslmode=prefer   ✅ 连上，客户端侧 SSL 生效 = False
sslmode=require  ❌ OperationalError: server does not support SSL, but SSL was required
SHOW ssl                  -> off
SHOW password_encryption  -> md5
```

`sslmode=prefer` 是 libpq 的**缺省值**，语义是「能加密就加密，**不能就明文连**」——
所以 DSN 里不写 `sslmode` 时不会有任何提示，连接照常成功，你不会知道它没加密。

口径要分清，别把话说过头：

- 口令**不是**明文过线 —— `password_encryption = md5`，走的是挑战应答，线上传的不是原文口令；
- 但 **查询语句与查询结果全程明文**，且 md5 认证本身早已不足以对抗离线爆破；
- 这是一条**公网**链路，中间人可读可改。

#### 开启后（2026-08-31 实测）

控制台侧已开启 SSL 加密，两个 DSN 都已带 `?sslmode=require`。实连回执：

```
（普通账号 polardb_maos，pg.env）
服务端 ssl 参数 : on
本连接走 SSL   : True

（高权限账号 maos_admin，pg-admin.env）
current_user   : maos_admin
服务端 ssl 参数 : on
本连接走 SSL   : True
```

验证方式是两边互相印证：服务端读 `show ssl`，客户端读 `c.pgconn.ssl_in_use`。
**只看其中一个都不够** —— 服务端 `on` 只说明它支持，不说明你这条连接真的用上了；
`sslmode=prefer` 下服务端照样是 `on` 而连接是明文，这正是开启前那一段的情形。

> ✅ **2026-08-31（T34 轨）起这条事实不必再另跑脚本**：`scripts/polardb_smoke.py`
> 第 1 步之后自带一行 `[note] SSL -> on/off`（读客户端侧 `pgconn.ssl_in_use`，
> psycopg2 回落 `conn.info.ssl_in_use`）。
>
> 在此之前，冒烟报告作为证据**在加密这一维上是哑的** —— 五步全绿不蕴含链路是加密的。
> 这一行走 `rep.note()`，**不计入成败、不进 `rows`、不影响退出码**，五步的断言强度
> 一点没动。已反向验证：在本机无 SSL 的 pgvector 容器上它如实显示 `off`
> —— 一个「恒为 on」的错误实现是看不出来的，必须有一次 off 才算读对了字段。

`require` 只加密不验证证书；要验证书就 `verify-ca` 并配 `sslrootcert`，本轮没做。

`pg-admin.env` 那条是**后补的**：开 SSL 那一轮的派单只写了改 `pg.env`，高权限账号
那条漏了，于是出现「实例侧 SSL 已开、`maos_admin` 仍走 `prefer`」的半吊子状态 ——
加密开着，但那条连接能明文就明文。2026-08-31 补齐并实连验证。
**这类缺口不会报错，只会安静地不生效**，和 `prefer` 本身的毛病是同一个。

#### 🔴 未闭环：口令没有轮换

开启 SSL 之前，这两个账号的口令在上面那条**明文公网链路**上用过多次。「按已暴露处理」
是当时就写下的结论，现在仍然成立 —— **开 SSL 不能追溯地保护开之前的流量。**

已决定不轮换（见 `docs/DECISIONS.md`）。这是**接受风险**，不是消除风险：

- 加密只对开启之后的连接生效；开启之前跑过的东西，该被看到的已经被看到了；
- `password_encryption = md5` 意味着线上传的不是原文口令，但 md5 早已不足以对抗
  离线爆破 —— 抓到过那几次握手的人，有离线还原的空间；
- 真要收口只有一条路：控制台重置两个账号的口令，然后更新两个 env 文件
  （口令让控制台只生成字母数字，避开 URL 编码那个坑）。

### 3.4 本轮验证没有覆盖的面

- 未验证**连接池 / 并发**行为，只用了单连接
- ~~未验证向量索引（`ivfflat` / `hnsw`）~~ → **HNSW 已验**，见 §1.5（构建耗时、召回、
  延迟、体积，两个规模点）。`ivfflat` 仍未验
- ~~未验证大数据量下的表现~~ → **已验到 20 万行**，见 §1.5。但数据是合成的，
  且**没有测并发**，所以只能回答「单查询在这个规模下多快」，回答不了「N 个并发下多快」
- ~~未验证 SSL / 加密连接要求~~ → 已验并已开启，见 §3.5。该实例**支持** SSL，
  2026-08-31 于控制台启用，两个账号的连接都实测走 SSL；但**口令未轮换**，
  明文期的暴露没有被撤销
- 本机对照组一共跑了三次、结果逐字一致：前两次跑在**已由 T10 轨起用的共享容器**上
  （未 `up` 也未 `down`，自建表以 `maos_smoke_` 前缀隔离并跑完即删），
  第三次跑在 T10 释放后**本轨自己新建的容器**上。三次输出相同，说明该结果与
  容器实例无关、可复现。理由与经过见 `docs/DECISIONS.md ## task-T14`

---

## 四、给人类的截图清单

评委要看的是**实例真实存在**的证据。

### 补跑已完成，这三张可以截了

1. 控制台实例详情页：**规格 + PG 大版本**（🔴 打码 host / 实例 ID）
2. **§1.2 那段五步全绿的终端输出** —— 脚本输出自带脱敏，可直接截。
   它里面的 `PolarDB 16.14.20.0 build 1f03f15d` 就是「这是真 PolarDB」的判据
3. 账号管理页：高权限账号存在 —— 这张的价值是配合 §1.3 说明那条前置条件

「插件管理页 `vector` 已启用」这张**截不出来也不必截**：实测该实例不需要在控制台启用
任何东西，`vector` 本来就在可用扩展列表里（§2.2）。

两条禁令仍然成立：

- ❌ 不要截本机 Docker pgvector 的输出去充当 PolarDB 的证据 —— §1.1 那段写着
  `PostgreSQL 16.15 (Debian ...)`，是 Debian 容器里的社区版 PG，懂行的评委一眼能看出来。
  要截就截 §1.2 那段。
- ❌ 不要拿控制台「实例存在」的页面去暗示「跑通过」—— 实例存在与能力验证是两件事，
  能力那半边的证据是 §1.2 的终端输出，不是控制台截图。

---

## 五、安全声明（铁律 7）

- DSN 与口令**只从环境变量读**，脚本不接受命令行传入连接串（命令行会进 shell 历史）。
- 本文件、`scripts/polardb_smoke.py`、两份账本中**不含任何真实 host、口令、实例 ID**，
  一律使用 `postgresql://<user>:<pass>@<host>:<port>/<db>` 这类占位形式。
- 脚本的脱敏做了双保险，并经**临时自验脚本实测**（假 host／口令／用户名／库名四样全被抹、
  泄漏条数 0、正文表名不误伤）：
  - host／口令／整条 DSN → 无条件子串替换为 `<redacted>`；
  - 用户名／库名 → 词边界替换（避免把 `maos_smoke_fts` 误伤成 `<redacted>_smoke_fts`）；
  - 兜底正则抹掉任何形如 `scheme://...@...` 的串；
  - **第 1 步（连接）失败时只报驱动异常类名、不报 message** —— 多数驱动会把 host
    拼进连接失败的 message 里。
- **SSL 已于 2026-08-31 启用**（§3.5）：两个 env 文件的 DSN 都带 `?sslmode=require`，
  实连验证 `ssl_in_use = True`。但 🔴 **开启之前的明文期已经发生，且口令没有轮换** ——
  那段时间这条公网链路上跑过的一切，仍应视为可被第三方看到。这是**已知并已接受的
  残留风险**，不是已经解决的问题。
- 高权限账号只用于 §1.3 那两条 DDL，**用完即可删除或改密** —— 日常连库一律用普通账号，
  它在 `GRANT` 之后已经够用。
- 🔴 **实例用完请立刻删除并收回白名单**：按量付费会一直计费，
  临时放开的 `0.0.0.0/0` 更不能留着。

---

## 六、成本优化清单（🔴 整份都是人类操作）

> **本节没有任何一步是会话能代做的，会话也没有执行过其中任何一步。**
> 缩容与删节点都是**不可逆的降配**，只有人能决定；控制台操作会话一律不碰。
>
> 下面每一条事实都来自本仓库已有记录（`docs/BACKLOG.md ## polardb-live` 的
> 2026-08-31 两条），**不是现查的** —— 整理这份清单时没有访问任何阿里域名。
> 费率会变，动手前请以控制台当时的实际报价为准。

### 6.0 先看这一张表：钱花在哪了

| 项 | 月费 | 占比 | 说明 |
| :-- | --: | --: | :-- |
| **存储（ESSD AutoPL 100 GB）** | **¥149.76** | **81.9%** | 实际只用了 **4.03 GB** |
| 只读节点 | ¥16.56 | 9.1% | |
| 其余（主节点等） | ¥16.56 | 9.0% | |
| 合计 | ¥182.88 | 100% | |

**结论先行：省钱的大头在盘上，不在节点上。** 存储缩容能省的钱是删只读节点的
**5.4 倍**，而且不牺牲任何可用性。**先做 6.1，再考虑 6.2。**

关键机制：ESSD AutoPL **按预购容量计费，不是按实际使用量**（官方原话「按照您预购的
ESSD 云盘的存储空间进行收费」）。那 96 GB 空盘是实收的钱 —— 不用也照付。

### 6.1 存储 100 GB → 40 GB（推荐先做，省 ¥89.86/月）

**第 1 步（人类，约 1 分钟就有答案）**
去控制台的**变配页**，看存储滑杆**能不能下拉到 40 GB**。

只需要看一眼，不必真提交。之所以要先看：标准版 + 按量付费**是否开放缩容**，
PG 侧文档没有正面写 —— 这是本清单唯一一个「仓库里给不出答案、只能看控制台」的点。
拉不动就到此为止，6.1 整条作废，直接去看 6.2。

**第 2 步（人类）** 拉得动的话，对着这四条约束逐条确认：

| 约束 | 当前是否满足 |
| :-- | :-- |
| AutoPL 地板 **40 GB**（想降到 20 GB 做不到） | ✅ 目标就是 40 |
| 缩容后总空间需比已用**至少多 20 GB** | ✅ 40 − 4.03 = 35.97 GB |
| 已用不得超过缩容后总空间的 **80%** | ✅ 4.03 / 40 ≈ 10% |
| AutoPL **不支持转换为其他存储类型** | ⚠️ 单向，改了回不去 |

**第 3 步（人类）** 挑个没人用的时间窗提交变配。**会有 ~30 秒闪断，且只读节点会自动重启** ——
别在演示或录制期间做。

**第 4 步（人类）** 变配完回来跑一次冒烟复验，确认没打破任何东西：

```bash
export MAOS_PG_DSN='postgresql://真实连接串'
python3 scripts/polardb_smoke.py        # 期望：5/5，exit=0，且 SSL 那行是 on
```

### 6.2 删只读节点（省 ¥16.56/月，🔴 有真实代价，不推荐单独为省钱做）

**代价是失去自动故障切换。** 每月省的 16.56 元买的是这个，请先确认这笔交易划算。

**第 1 步（人类，🔴 删之前先做，不是删之后）**
把连接串从**集群地址（rwlb）换成主地址**，两个 env 文件都要换：
`~/.config/maos/pg.env`、`~/.config/maos/pg-admin.env`。

为什么必须先换：**零只读节点时集群地址的行为是灰区** —— 官方文档没有正面写
「集群中不存在只读节点时集群地址如何表现」，删节点那篇只说「该节点上的连接会发生
闪断，其他节点不受影响」，没有任何「集群地址会失效」的警告。
推断是集群地址仍可用、读写都落主节点（依据：集群地址的节点集合始终含主节点），
**但这是推断，不是文档原文**。而主地址的语义是文档明写死的（永远指向主节点、读写都行）。
**别拿生产连接去赌灰区行为。**

**第 2 步（人类）** 换完先验证再删节点：

```bash
python3 scripts/polardb_smoke.py        # 主地址上先跑到 5/5、SSL=on，再去删节点
```

**第 3 步（人类）** 控制台删只读节点。

**第 4 步（人类）** 再跑一次冒烟确认仍是 5/5。

### 6.3 最省的那条路：不用的时候把实例删掉

按量付费**一直在计费**，跟你用不用没关系。复赛材料需要的证据（§1.2 的五步全绿输出、
§1.4 的中文召回表、§1.5 的 HNSW 读数）**已经全部落盘在本文件里**，
不需要实例一直活着来背书。

要复现时重建一台再跑 §2.3 那一条命令即可 —— 代价是重走一遍 §1.3 的高权限账号 DDL
和白名单放行。**这条比 6.1 + 6.2 加起来省得多**，只是牺牲「随时能连上去看看」。
